@model IEnumerable<FlatProject7.Models.Counter>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SerialNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LastCheckDate)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.NextCheckDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SerialNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LastCheckDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NextCheckDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { item.Id/* id=item.PrimaryKey */ }) |
        </td>
    </tr>
}

</table>
